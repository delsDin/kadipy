# KadiPy V1 – Dépendances
# Compatible Python >=3.9

# ========================
# Dépendances principales
# ========================

# Requêtes HTTP pour les APIs météo et téléchargement de datasets de prix
requests>=2.25

# Manipulation de données tabulaires, lecture/écriture Excel/CSV, jointures
pandas>=1.3

# Support de calculs numériques (GDD, régression) et dépendance de pandas/scikit-learn
numpy>=1.21

# Lecture/écriture des fichiers Excel .xlsx (coopératives)
openpyxl>=3.0

# Lecture des anciens fichiers .xls encore fréquents (format OLE)
# ⚠️ La version 2.0+ ne supporte plus les .xls, on reste en <2.0
xlrd<2.0

# Validation et nettoyage de coordonnées GPS (distance, correction d'erreurs)
geopy>=2.2

# Modèles de prédiction (régression linéaire, random forest léger) pour les prix
scikit-learn>=1.0

# Sauvegarde/chargement efficace des modèles ML dans le cache offline
joblib>=1.1

# (optionnel) Utilitaire de fuseau horaire à partir des coordonnées – non requis en V1
# tzwhere>=3.0

# ==========================
# Développement & packaging
# ==========================

# Framework de tests unitaires
pytest>=7.0

# Mocking dans les tests (fixtures et fonctions)
pytest-mock>=3.7

# Mock des appels HTTP dans les tests
responses>=0.21

# Alternative pour mocker les requêtes HTTP
requests-mock>=1.9

# Générateur de site de documentation statique
mkdocs>=1.3

# Thème moderne pour la documentation
mkdocs-material>=8.0

# Construction du paquet (pyproject.toml)
setuptools>=60
wheel>=0.37

# Lecture et traitement NetCDF
xarray>=2023.1
netCDF4>=1.6
dask>=2023.1

# Détection d'encodage
chardet>=5.0

# ==========================
# Dépendances du module kidas
# ==========================

# Calculs statistiques (Z-score, MAD) pour DataCleaner et DataValidator
scipy>=1.9

# Création de géométries GPS (points shapely) pour DataNormalizer
shapely>=2.0

# Suppression des accents unicode pour DataCleaner et DataNormalizer
Unidecode>=1.3